VITE_API_URL=http://localhost:3000
# apps/mcp runs as a separate deployable service (not served by apps/api's
# static SPA hosting), so its URL needs its own env var per environment.
VITE_MCP_URL=http://localhost:3002
