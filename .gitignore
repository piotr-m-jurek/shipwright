# deps
node_modules/

# env files — all packages
.env
**/.env
.env.local
**/.env.local

# compiled output
**/dist/

# PR Lens writes its previews here. They are rebuilt on demand.
.pr-lens/

# Claude Code local machine state — per-developer, not shared
.claude/settings.local.json
.claude/scheduled_tasks.lock
